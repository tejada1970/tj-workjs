import Contact from '@/components/Contact';

export const metadata = {
  title: 'Work JS | Contact',
}

const ContactPage = () => {
  return (
    <div>
      <Contact />
    </div>
  )
}
  
export default ContactPage